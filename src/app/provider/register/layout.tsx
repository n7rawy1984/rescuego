import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Join as Recovery Provider',
  description: 'Register as a roadside recovery provider on RescueGo. Serve drivers across the UAE with verified, on-demand recovery requests.',
  alternates: { canonical: 'https://rescuego.ae/provider/register' },
  openGraph: {
    title: 'Join RescueGo as a Recovery Provider',
    description: 'Register and start receiving roadside recovery requests across the UAE.',
    url: 'https://rescuego.ae/provider/register',
  },
}

export default function ProviderRegisterLayout({ children }: { children: React.ReactNode }) {
  return children
}
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Join as Recovery Provider',
  description: 'Register as a roadside recovery provider on RescueGo. Serve drivers across the UAE with verified, on-demand recovery requests.',
  alternates: { canonical: 'https://rescuego.ae/provider/register' },
  openGraph: {
    title: 'Join RescueGo as a Recovery Provider',
    description: 'Register and start receiving roadside recovery requests across the UAE.',
    url: 'https://rescuego.ae/provider/register',
  },
}

export default function ProviderRegisterLayout({ children }: { children: React.ReactNode }) {
  return children
}
